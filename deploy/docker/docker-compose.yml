# Usage
#   Start:              docker compose up
#   Stop:               docker compose down
#   Destroy:            docker compose down -v --remove-orphans

name: refly

include:
  - path: docker-compose.middleware.yml

services:
  api:
    extends:
      file: docker-compose.services.yml
      service: api
    image: refly-api:latest

  web:
    extends:
      file: docker-compose.services.yml
      service: web
    image: refly-web:latest
    ports:
      - 5700:80
